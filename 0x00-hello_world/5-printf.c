#include <stdio.h>
/**
 * main - A program that prints a line with printf function
 * Description: 'the program prints a line with printf'
 * Return : 0 (always)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
